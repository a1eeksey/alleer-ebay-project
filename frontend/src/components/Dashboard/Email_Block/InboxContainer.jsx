import React from "react";
import PageTitle from "../PageTitle";

function InboxContainer({pageTitle}) {
  return (
    <section>
      <PageTitle pageTitle={pageTitle}/>
        <div>
          
        </div>
    </section>
  );
}

export default InboxContainer;

